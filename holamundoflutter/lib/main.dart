import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Bienvenido sssss',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Bienvenido a AAAAA'),
        ),
        body: Center(
          child: Text('Hola Mundddddddddddo'),
        ),
      ),
    );
  }
}
